# Elasticsearch Configuration
ELASTIC_URL=https://your-elasticsearch-instance.com:9200
ELASTIC_API_KEY=your_api_key_here

# Elasticsearch Index
ELASTIC_INDEX=documents

# Elasticsearch Inference ID (for image description embeddings)
# This should match the inference endpoint configured in your Elasticsearch instance
INFERENCE_ID=my-embedding-model
